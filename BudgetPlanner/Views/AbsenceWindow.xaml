<Window x:Class="BudgetPlanner.Views.AbsenceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BudgetPlanner.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="AbsenceWindow" Height="350" Width="250">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Register Absence"
             Style="{StaticResource HeaderTitle}"
             Margin="0 0 0 20"/>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2">
            <TextBlock Text="Monthly Salary:" FontWeight="Bold"/>
            <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:N2}, ValidatesOnExceptions=True}"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Start Date" FontWeight="Bold" Margin="3 20 3 0"/>
        <DatePicker Grid.Row="3" Grid.Column="0" SelectedDate="{Binding StartDate}"/>

        <TextBlock Grid.Row="2" Grid.Column="1" Text="End Date" FontWeight="Bold" Margin="3 20 3 0"/>
        <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding EndDate}"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Margin="0 20 0 20">
            <TextBlock Text="Type of absence" FontWeight="Bold"/>
            <RadioButton Content="VAB"
             IsChecked="{Binding IsVab}"
             Margin="0 0 10 0" FontStyle="Italic"/>
            <RadioButton Content="Sick Leave"
             IsChecked="{Binding IsSickness}"
             Margin="0 0 10 0" FontStyle="Italic"/>
        </StackPanel>

        <Grid Grid.Row="5" Grid.ColumnSpan="2" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Save" 
                    Command="{Binding SaveCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Style="{StaticResource PrimaryButton}"
                    Width="100"/>
            <Button Grid.Column="1" Content="Cancel"
                    Style="{StaticResource PrimaryButton}"
                    Background="Gray"
                    IsCancel="True"
                    Width="100"/>
        </Grid>
    </Grid>
</Window>
